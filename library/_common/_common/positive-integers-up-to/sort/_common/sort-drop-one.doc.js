/*
	eslint-disable

	import-x/unambiguous,
	unicorn/no-empty-file,
	unicorn/prevent-abbreviations
	--
	jsdoc
*/

/**
 * @template {any[]} xs
 * @typedef {xs extends [any, ...infer tail] ? tail : []} SortDropOne
 */
